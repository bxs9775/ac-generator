{
  "name": "ac-generator",
  "version": "1.0.0",
  "description": "Personal project investigating using tools like twinery and inflection.js to generate Animal Crossing villager dialogue.",
  "main": "./src/app.js",
  "scripts": {
    "start": "ts-node-dev --respawn --pretty --transpile-only ./src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "bundle": "browserify -p esmify --no-dedupe ./src/scripts/app.js -o ./hosted/scripts/app.js"
  },
  "author": "Brian Sandon",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.4",
    "express": "^4.18.2",
    "jquery": "^3.6.4",
    "tracery-grammar": "^2.7.4"
  },
  "devDependencies": {
    "@types/body-parser": "^1.19.2",
    "@types/express": "^4.17.17",
    "@types/jquery": "^3.5.16",
    "@types/node": "^18.15.5",
    "@types/tracery-grammar": "^2.7.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.0.2"
  }
}
